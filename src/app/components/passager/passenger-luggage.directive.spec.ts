import { PassengerLuggageDirective } from './passenger-luggage.directive';

describe('PassengerBandingDirective', () => {
  it('should create an instance', () => {
    const directive = new PassengerLuggageDirective();
    expect(directive).toBeTruthy();
  });
});
