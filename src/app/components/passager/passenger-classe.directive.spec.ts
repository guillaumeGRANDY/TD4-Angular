import { PassengerClasseDirective } from './passenger-classe.directive';

describe('PassengerClasseDirective', () => {
  it('should create an instance', () => {
    const directive = new PassengerClasseDirective();
    expect(directive).toBeTruthy();
  });
});
